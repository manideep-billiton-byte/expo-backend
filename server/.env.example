# Backend environment variables for real email/SMS integration

# Server
PORT=5000
# Base URL for invite links and organization login
# For AWS Production: https://d2ux36xl31uki3.cloudfront.net
# For Local Development: http://localhost:5173
INVITE_LINK_BASE=https://d2ux36xl31uki3.cloudfront.net

# Postgres
# Option A: single URL
# DATABASE_URL=postgres://postgres:password@localhost:5432/expo_db
#
# Option B: individual vars
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=expo_db
# Set to 'require' if your Postgres requires SSL (e.g. cloud providers)
PGSSLMODE=disable

# SMTP (for nodemailer - legacy, replaced by AWS SES)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_smtp_user
# SMTP_PASS=your_smtp_password
# SMTP_FROM="Your App <no-reply@example.com>"

# Twilio (for SMS - legacy, replaced by AWS SNS)
# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_AUTH_TOKEN=your_twilio_auth_token
# TWILIO_FROM=+1234567890

# GST Verification API
# GST_API_KEY=your_gst_api_key_here

# AWS Configuration (for SES email and SNS SMS)
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here
AWS_REGION=ap-south-1

# AWS SES - Use a VERIFIED email address
SES_FROM_EMAIL=deepak@btsind.com

# AWS SNS - Optional sender ID
SNS_SENDER_ID=EXPO
